name: WatchOS RHR Build

on:
  push:
    branches: 
      - main
  pull_request:
    types: [opened, reopened]

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Cache Swift packages
        uses: actions/cache@v4
        with:
          path: .build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-
      
      - name: Select Xcode 16.0
        run: sudo xcode-select -s /Applications/Xcode_16.app

      - name: Check Xcode version
        run: xcodebuild -version

      - name: Build
        run: |
            xcodebuild clean build -project RHR.xcodeproj -scheme 'RHR Watch App' -destination 'platform=watchOS Simulator,id=7E4F0B48-4606-4208-B6E1-A1FBCA93A4C5,OS=11.0,name=Apple Watch Series 10 (46mm)'

      - name: Test
        run: |
            xcodebuild test -project RHR.xcodeproj -scheme 'RHR Watch App' -destination 'platform=watchOS Simulator,id=7E4F0B48-4606-4208-B6E1-A1FBCA93A4C5,OS=11.0,name=Apple Watch Series 10 (46mm)'

